# Server Configuration
PORT=3000
NODE_ENV=production

# External URL (for WebSocket connections from outside network)
# Leave blank for local-only, or set to your deployment URL
EXTERNAL_URL=http://localhost:3000
